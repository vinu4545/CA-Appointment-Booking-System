export async function connectGoogleCalendar() {
  return {
    connected: false,
    message: 'Google Calendar integration placeholder ready for backend wiring.',
  };
}

export async function syncAppointmentToCalendar() {
  return {
    synced: false,
    message: 'Calendar sync is currently mocked on the frontend.',
  };
}
